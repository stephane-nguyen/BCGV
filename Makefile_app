CC = gcc
CFLAGS =-W -Wall -pedantic
LDFLAGS = -L. -ldrapi
TARGET = app

all: $(TARGET)

$(TARGET): app.o
    $(CC) $(CFLAGS) -o $(TARGET) app.o $(LDFLAGS)

app.o: app.c data.h
    $(CC) $(CFLAGS) -c app.c

clean:
    rm -f *.o $(TARGET)
