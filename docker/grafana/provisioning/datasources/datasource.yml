apiVersion: 1
datasources:
  - name: InfluxDB
    type: influxdb
    access: server
    database: mydb
    user: $INFLUXDB_USERNAME
    secureJsonData:
        password: $INFLUXDB_PASSWORD
    url: http://influxdb:8086
    isDefault: true
    editable: true
